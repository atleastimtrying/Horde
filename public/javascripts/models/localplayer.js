// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.LocalPlayer = (function(_super) {

    __extends(LocalPlayer, _super);

    function LocalPlayer() {
      return LocalPlayer.__super__.constructor.apply(this, arguments);
    }

    LocalPlayer.prototype.initialize = function(app) {
      this.app = app;
      this.x = Math.random() * 600;
      this.y = Math.random() * 400;
      this.angle = Math.random() * 360;
      this.acceleration = 2;
      this.rotationSpeed = 1;
      this.health = 10;
      this.ammo = 20;
      this.player = true;
      this.rotationModifier = 90;
      this.instructions = [0, 0];
      return this.bindings();
    };

    LocalPlayer.prototype.bindings = function() {
      $(this.app).bind('leftclick', this.leftclick);
      $(this.app).bind('rightclick', this.rightclick);
      $(this.app).bind('up', this.up);
      $(this.app).bind('down', this.down);
      $(this.app).bind('left', this.left);
      $(this.app).bind('right', this.right);
      $(this.app).bind('bomb', this.bomb);
      return $(this.app).bind('pause', this.pause);
    };

    LocalPlayer.prototype.up = function() {
      this.y -= 1;
      this.draw();
      return this.transmit();
    };

    LocalPlayer.prototype.down = function() {
      return this.y += 1;
    };

    LocalPlayer.prototype.left = function() {
      return this.x -= 1;
    };

    LocalPlayer.prototype.right = function() {
      return this.x += 1;
    };

    LocalPlayer.prototype.draw = function() {};

    LocalPlayer.prototype.transmit = function() {};

    return LocalPlayer;

  })(Backbone.Model);

}).call(this);
